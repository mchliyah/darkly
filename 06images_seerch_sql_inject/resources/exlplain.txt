the same as the memberserch tests the diffrent here that there is no promt to show errors , but when we just test to show all table data we got it 

1 OR 1=1 UNION SELECT table_name, column_name FROM information_schema.columns

1 OR 1=1 UNION SELECT comment, title FROM list_images